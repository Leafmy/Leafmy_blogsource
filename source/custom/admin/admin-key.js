/* ============================================================
   管理页密钥（只存 SHA-256 哈希，明文不落源码）
   ------------------------------------------------------------
   - 明文密钥由站长持有；在检索栏输入该密钥即可进入 /admin/
   - 换密钥：node -e "console.log(require('crypto').createHash('sha256').update('新密钥').digest('hex'))"
     然后替换下面的值（管理页「设置 → 修改管理员密钥」也能自动提交）
   - 注意：静态站点没有后端，这个哈希只是"门帘"，能挡住顺手访问的人；
     真正保护数据的是管理页里配置的 GitHub Token（只存在你浏览器里）。
   ============================================================ */
window.ADMIN_KEY_SHA256 = 'c58abdfa33eaf89931143adab8593aea9e62e2b5f0244f55882cc0d44013b84b'
